spring:
  application:
    name: SurveyApp
  datasource:
    url: ${MYSQL_ROOT}
    username: ${MYSQL_USERNAME}
    password: ${MYSQL_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8880

app:
  secret: ${SECRET_KEY}